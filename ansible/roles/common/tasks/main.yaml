- name: Update apt cache
  apt: update_cache=yes cache_valid_time=3600

- name: Ensure basic packages installed
  apt:
    name: [git, curl, unzip, python3-pip]
    state: present

- name: Create app user
  user:
    name: appuser
    shell: /bin/bash
    state: present
    create_home: yes